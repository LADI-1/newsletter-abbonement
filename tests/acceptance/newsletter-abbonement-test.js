import { module, test } from 'qunit';
import { visit, currentURL } from '@ember/test-helpers';
import { setupApplicationTest } from 'ember-qunit';

module('Acceptance | newsletter abbonement', function(hooks) {
  setupApplicationTest(hooks);

  test('should show main as the home page', async function (assert) {
  });
  test('should link to newsletter.', async function (assert) {
  });

  test('should link to fachbeitraege.', async function (assert) {
  });
  test('should link to mission.', async function (assert) {
  });
  test('should link to konzept.', async function (assert) {
  });
  test('should link to relevanz.', async function (assert) {
  });
  test('should link to kontakt.', async function (assert) {
  });
});
